{{#each heroes}}
  <div class="hero-temp">
    <img class="js-hero-pic" src="{{image}}" alt=""/>
    <br/>
    <h3>{{name}}</h3>
  </div>
{{/each}}


{{#each dragons}}
  <div class="dragon-temp">
    <img class="js-dragon-pic" src="{{image}}" alt=""/>
    <br/>
    <h3>{{name}}</h3>
  </div>
{{/each}}
